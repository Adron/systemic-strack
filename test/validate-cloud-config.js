var assert = require('assert');

describe('this thing', function () {

  describe('called math', function () {
    it('should always work', function () {
      assert.equal(-1, [1,2,3].indexOf(5));
    });
  });

});